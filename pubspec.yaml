name: deeply
description: Modify (update/unset/rename) deeply nested maps by using a keyPath-string to access sub-maps recursively
version: 1.2.0
homepage: https://github.com/marioreggiori/deeply

environment:
  sdk: ">=2.12.0 <3.0.0"

dev_dependencies:
  test: any
